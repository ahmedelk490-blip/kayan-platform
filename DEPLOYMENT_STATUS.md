# DEPLOYMENT_STATUS — حالة النشر

> تاريخ: 2026-08-11 · كل رقم هنا مقيس من الخادم الحيّ أو من طلب HTTP فعلي.

---

## المنشور الآن

| ما هو | أين | الحالة |
|---|---|---|
| الموقع التسويقي | `kayan-uniform.com` | ✅ حيّ |
| نظام ERP | — | ❌ غير منشور على أي نطاق |

### آخر تحقّق حيّ

| الفحص | النتيجة |
|---|---|
| 7 مسارات عامة (بتجاوز الكاش) | كلها 200 |
| 21 صورة منتج + الشعار | كلها 200 |
| بصمة البناء الحيّة = المنشورة | ✅ `bikocA68p-OtXtRg7SPmZ` |
| ترويسة الكاش | `s-maxage=120` |
| نموذج طلب العرض | 201، ورفض الناقص 422 |
| الطلب يُحفظ خارج مجلد النشر | ✅ `~/kayan-leads/` |
| أرشيف المصدر العام | ✅ 404 |

---

## بنية الخادم

```
~/domains/kayan-uniform.com/
├── app/                 ← مجلد الإنتاج الثابت (ملكي)
│   ├── .next/           ← ناتج البناء، يُبدَّل ذرّياً
│   ├── node_modules/    ← يُثبَّت على لينكس
│   ├── server.mjs       ← نقطة إقلاع Passenger
│   └── .env
├── public_html/
│   └── .htaccess        ← توجيه Passenger → app/
└── (hbuilds/ حُذف)
~/kayan-leads/leads.jsonl ← خارج أي مجلد نشر
```

`hbuilds/versions/` **لم يعد مستخدماً**. كان الموقع يعمل منه، وهو مجلد مؤقت
تملكه منظومة هوستنجر وقد تحذفه.

---

## أمر النشر

```bash
KAYAN_SSH_KEY=<path> node scripts/deploy-site.mjs
```

يبني محلياً → يطابق إصدارات الحزم → يضغط → يرفع → يبدّل ذرّياً → يعيد التشغيل
→ يتحقّق بتجاوز الكاش ومطابقة بصمة البناء.

**البناء محلي إجباراً**: `next build` يموت على الخادم بـ`EAGAIN` عند أول عامل
(حدّ عمليات CloudLinux)، حتى مع `cpus: 1`. لكن `npm install` ينجح، فالملفات
الثنائية تُختار على لينكس صحيحة.

---

## قائمة التحقّق المطلوبة — الحالة الصريحة

| # | المطلوب | الحالة |
|---|---|---|
| 1 | البناء | ✅ في السكربت |
| 2 | lint | ✅ **بوّابة قبل أي اتصال بالخادم** |
| 3 | typecheck | ✅ **بوّابة قبل أي اتصال بالخادم** |
| 4 | الاختبارات | ✅ 7 مجموعات تحقّق، والفشل يوقف النشر |
| 5 | نشر ذرّي | ✅ `.next.new` ثم `mv` |
| 6 | إعادة التشغيل | ✅ `tmp/restart.txt` |
| 7 | تحقّق حيّ | ✅ بتجاوز الكاش |
| 8 | تحقّق من الكاش | ✅ `s-maxage=120` |
| 9 | تحقّق من قاعدة البيانات | ❌ **لا ينطبق — لا قاعدة بيانات على هذا المضيف** |
| 10 | تحقّق من المصادقة | ❌ **لا ينطبق — النظام غير منشور** |

البنود 2–4 ستُدمج. البندان 9–10 محجوبان بعائق المضيف لا بالكود.

---

## العائق الحاكم

**الاستضافة المشتركة لا توفّر PostgreSQL.** مقيس على الجهاز:

```
المنفذ 5432                → مغلق
/opt/alt/postgresql11      → مكتبة عميل PHP، لا خادم
المتاح                     → MariaDB 11.8.8
```

نظام ERP يعزل المستأجرين بـRow-Level Security على 55 جدولاً. **MariaDB لا تملك
RLS إطلاقاً** — النقل إليها حذف لطبقة الأمان لا ترحيل لها.

فـ`/dashboard` و`/sales` و`/admin` و`/portal` **لا يمكن أن تعمل على هذا المضيف
بأي قدر من العمل البرمجي**. تحتاج VPS فيه PostgreSQL 17.

---

## مخاطر قائمة

| الخطر | الحالة |
|---|---|
| لا CI — 387 تأكيداً تُشغَّل يدوياً | مفتوح |
| لا نسخ احتياطي مجدول | مفتوح |
| `kayan_auth` يحمل `BYPASSRLS` مع DML على كل الجداول | مفتوح منذ المرحلة 7 |
| الطلبات في ملف غير مشفّر بدل CRM | مفتوح — يُغلق بالتصحيح رقم 3 |
| كلمات مرور التطوير ظاهرة في `.env` | مفتوح |

---

## تنبيه تشغيلي

**لا تضغط Deploy في hPanel.** ستنشئ منظومة هوستنجر مجلد إصدار جديداً وقد تعيد
كتابة `.htaccess` بعيداً عن `app/`، فتتضارب المنظومتان.

**حدّ SSH**: الخادم يقطع الاتصال بعد كثرة الجلسات المتتابعة؛ يتعافى خلال دقائق.
