# DEPLOYMENT_STATUS — حالة النشر

> تاريخ: 2026-09-01 · كل رقم هنا مقيس من الخادم الحيّ أو من طلب HTTP فعلي.
> (نسخة 2026-08-11 القديمة كانت تصف بنية `app/` اليدوية — **حُذفت من الخادم في 18 أغسطس** ولم يعد شيء منها قائماً.)

---

## المنشور الآن

| ما هو | أين | الحالة |
|---|---|---|
| المنصّة الموحّدة (الموقع العام + نظام ERP كاملاً) | `kayan-uniform.com` | ✅ حيّة — تطبيق واحد |

آخر تحقّق (2026-08-31 22:00 UTC): الكوميت المنشور `f3fc9ee` يطابق رأس `main`،
وبصمة البناء `H0cqrjtEIkF-VQzdeMozV`، ومسار `/returns/[id]` الجديد يرد 307
لتسجيل الدخول (في البناء السابق كان 404).

---

## طريقة النشر الوحيدة: GitHub ← منظومة هوستنجر المُدارة

```
git push origin main  →  هوستنجر يسحب ويبني (10–15 دقيقة)  →  حيّ
```

لا SSH ولا رفع يدوي ولا ضغطة زر. الدفعة وحدها تكفي، والبناء عند هوستنجر
(البناء المحلي ما زال مستحيلاً على العتاد المشترك — `EAGAIN`).

### بنية الخادم الفعلية (مقيسة من `.htaccess` والقرص)

```
~/domains/kayan-uniform.com/
├── hbuilds/
│   ├── current → versions/<uuid>/   ← رابط رمزي يتبدّل مع كل نشر
│   │   └── nodejs/                  ← ناتج standalone: server.js + apps/ + node_modules
│   ├── last-source/                 ← نسخة git التي بُني منها (فيها الكوميت)
│   ├── config/                      ← ملفات المنظومة (preload-timestamp.js …)
│   └── logs/
├── public_html/.htaccess            ← Passenger → hbuilds/current/nodejs · server.js
└── DO_NOT_UPLOAD_HERE               ← علامة: لا ترفع شيئاً يدوياً هنا
```

- Passenger يشغّل `server.js` (ناتج `output: 'standalone'`) بـ Node ‏20.19.4.
- `.next` الفعلي داخل `nodejs/apps/web/.next` (التتبّع من جذر الـmonorepo).
- **متغيّرات البيئة من شاشة hPanel فقط** — لا يوجد أي ملف `.env` على الخادم.

### التحقّق بعد أي نشر

```bash
# الكوميت المنشور (من جهازك):
ssh -p 65002 u257117736@185.206.160.134 \
  "cd ~/domains/kayan-uniform.com/hbuilds/last-source && git log --oneline -1"
```

وقارنه بـ`git log --oneline -1` محلياً. مفتاح SSH: ‏`~/.ssh/id_ed25519` (مصادقته مقيسة وتعمل؛
الجلسات التفاعلية الطويلة تُقتل بحدّ CloudLinux — الأوامر القصيرة تمرّ).

---

## قاعدة البيانات

`prisma/schema.prisma` على **MySQL/MariaDB** منذ 19 أغسطس (`migrate-to-mysql.mjs`) —
قرار واعٍ بعد قياس أن المضيف لا يوفّر PostgreSQL. عزل المستأجرين انتقل من
RLS إلى تصفية `tenantId` في طبقة التطبيق. الاتصال يُضبط في متغيّرات hPanel،
وتغييرات المخطط تُطبَّق بـ`prisma db push` على قاعدة الإنتاج (لا ملفات هجرة
جديدة منذ `20260824240000_product_dozen`).

---

## سكربتات النشر القديمة — لا تستعملها للنشر

| السكربت | حاله |
|---|---|
| `scripts/deploy-site.mjs` | **أثري.** يستهدف `~/domains/…/app/` المحذوف. أُبقي لأن خط تحزيمه (lint → typecheck → بناء الحزمة المسطّحة) ما زال أسرع فحص «هل سيبني عند هوستنجر؟» قبل الدفع |
| `scripts/package-hostinger.mjs` | يعمل (أُصلح 2026-09-01: توليد Prisma client + تضمين `apps/web/scripts`) |

**لا تضغط Deploy يدوياً في hPanel ولا ترفع ملفات إلى `public_html`** —
المنظومة تدير `hbuilds/` بالكامل، والدفعة إلى GitHub هي الطريق الوحيد.

---

## مخاطر قائمة

| الخطر | الحالة |
|---|---|
| لا CI — مجموعات التحقّق تُشغَّل يدوياً | مفتوح |
| لا نسخ احتياطي مجدول لقاعدة الإنتاج | مفتوح |
| العزل بين المستأجرين في طبقة التطبيق فقط (لا RLS في MariaDB) | مفتوح — مقبول بمستأجر واحد |
| فحص «هل نجح النشر؟» يدوي (أمر SSH أعلاه) | مفتوح |
