# ARCHITECTURE_STATUS — تدقيق المعمارية الحالية

> تاريخ التدقيق: 2026-08-11 · كل بند هنا **مقيس** من الكود أو من الخادم الحيّ،
> لا مستنتج من الذاكرة. ما لم أتحقّق منه مكتوب أنه غير متحقَّق منه.

---

## 1. المعمارية الحالية

مستودع واحد (Turborepo + npm workspaces) يحوي **تطبيقَي Next.js 15 منفصلين**
وستّ حزم مشتركة.

```
erp-platform/
├── apps/
│   ├── marketing/   الموقع العام   → المنفذ 3200
│   └── web/         نظام ERP       → المنفذ 3300
├── packages/
│   ├── brand/       الهوية والشعار      (يستخدمه الاثنان)
│   ├── domain/      منطق الأعمال        (ERP فقط)
│   ├── motion/      الحركة              (الموقع فقط)
│   ├── ui-market/   مكوّنات الموقع      (الموقع فقط)
│   └── utils/       أدوات               (الاثنان)
└── prisma/          مخطط واحد · 8 هجرات
```

**تطبيقان، لا نظامان.** المنطق والمخطط والمصادقة موجودة مرة واحدة فقط.

---

## 2. النطاقات والمسارات

| العنوان | الحالة الفعلية |
|---|---|
| `kayan-uniform.com/` | ✅ حيّ — الموقع التسويقي |
| `kayan-uniform.com/contact` | ✅ حيّ |
| `kayan-uniform.com/legal/{privacy,terms}` | ✅ حيّ |
| `kayan-uniform.com/login` | ⚠️ حيّ لكنه **صفحة تحويل** لا شاشة دخول |
| `kayan-uniform.com/about` | ❌ غير موجود |
| `kayan-uniform.com/products` | ❌ غير موجود |
| `kayan-uniform.com/services` | ❌ غير موجود |
| `/dashboard` `/sales` `/admin` `/portal` | ❌ **غير منشورة على أي نطاق** |

نظام ERP لم يُنشر قط. يعمل محلياً فقط.

---

## 3. تطبيقات الواجهة

**`apps/marketing`** — 5 صفحات، مسار خادم واحد (`/api/leads`).
اعتمادياته: `@erp/brand`, `@erp/motion`, `@erp/ui-market`, `@erp/utils`, `motion`, `next`, `react`.
**لا يعتمد على Prisma ولا يملك أي وصول لقاعدة البيانات.**

**`apps/web`** — 23 مساراً: `admin`, `catalog`, `customers`, `damage`, `dashboard`,
`expenses`, `formulas`, `inventory`, `invoices`, `login`, `manufacturing`, `portal`,
`products`, `purchasing`, `reports`, `sales`, `settings`, `suppliers`, `supplies`,
`warehouses`.
اعتمادياته تضم `@prisma/client`, `@node-rs/argon2`, `zod`, `@erp/domain`.

---

## 4. الخلفية

لا خادم منفصل. Next.js Server Actions ومعالجات مسارات، وPrisma 6.19.3 هو
طبقة الوصول الوحيدة. المنطق المالي في `packages/domain` **خالٍ من أي إطار**
ومُختبَر بـ 387 تأكيداً عبر 14 مجموعة.

---

## 5. قاعدة البيانات

**PostgreSQL 17.5** — مخطط واحد، 8 هجرات، أعمدة `NUMERIC(19,4)` للمبالغ والكميات.

- **Row-Level Security مفعّلة ومفروضة (`FORCE`) على 55 جدولاً**، بسياسات تعتمد
  على `app.tenant_id`، ومُثبَتة بـ20 تأكيداً تهاجم العزل لا تفترضه.
- ثلاثة أدوار قاعدة بيانات: `kayan_app` (بلا `BYPASSRLS`)، `kayan_owner`،
  `kayan_auth` (يحمل `BYPASSRLS` للهوية وحدها).

⚠️ **الموقع التسويقي لا يلمس قاعدة البيانات إطلاقاً.**

---

## 6. المصادقة

**نظام واحد، ولا يوجد ثانٍ.**

- `apps/web/src/lib/auth.ts` — Argon2id، رموز جلسات مُجزّأة، تسجيل دخول مُتساوي
  الزمن، قفل بعد محاولات فاشلة.
- `apps/web/src/lib/guard.ts` — `requirePermission` مع **منع افتراضي**، ويضع
  المستأجر في سياق قاعدة البيانات.
- `packages/domain/src/rbac.ts` — 43 صلاحية، 4 أدوار.

| الدور | مسار الهبوط |
|---|---|
| ADMIN | `/admin` |
| MANAGER | `/dashboard` |
| SALES | `/sales` |
| CUSTOMER | `/portal` |

**صفحة `/login` في الموقع التسويقي لا تحتوي كلمة مرور ولا جلسة ولا argon —
هي صفحة تحويل فقط.** لا توجد مصادقة مكرّرة ولا شاشة دخول وهمية.

---

## 7. النشر على هوستنجر

- استضافة **مشتركة** (`u257117736`)، لا VPS.
- الموقع يعمل من `~/domains/kayan-uniform.com/app/` — مجلد ثابت، عبر Passenger
  من `.htaccess`. **لم يعد يعتمد على `hbuilds/versions/`** (حُذف).
- الطلبات تُحفظ في `~/kayan-leads/` خارج مجلد النشر.
- **لا يوجد PostgreSQL.** مقيس على الجهاز: المنفذ 5432 مغلق،
  و`/opt/alt/postgresql11` مكتبة عميل PHP لا خادم، والمتاح MariaDB 11.8.
- **البناء مستحيل على الخادم**: `next build` يموت بـ`EAGAIN` عند أول عامل
  (حدّ عمليات CloudLinux)، حتى مع `cpus: 1`. البناء محلي والرفع للناتج.

---

## 8. ما هو موصول بشكل صحيح بالفعل

| العنصر | الحالة |
|---|---|
| مصادقة واحدة | ✅ لا يوجد نظام ثانٍ |
| RBAC واحد | ✅ مصدر واحد في `packages/domain` |
| مخطط قاعدة بيانات واحد | ✅ `prisma/schema.prisma` |
| نظام هوية واحد | ✅ `@erp/brand` يستخدمه التطبيقان |
| مسارات ERP المطلوبة | ✅ `/login` `/dashboard` `/sales` `/admin` `/portal` موجودة |
| عزل المستأجرين | ✅ RLS مفروضة على 55 جدولاً |
| نطاق واحد للموقع | ✅ `kayan-uniform.com` |

---

## 9. ما هو مكرّر — وهذا لبّ المشكلة

### 9.1 كتالوج المنتجات — تكرار حقيقي
`apps/marketing/src/site.ts` يحوي `PRODUCTS` **مكتوبة يدوياً في الكود**:
الاسم والوصف والمجلد وعدد الصور. وفي قاعدة البيانات جدول `Product` فيه 7 منتجات
بأسعارها ومتغيّراتها.

**تغيير المدير لمنتج في النظام لا يظهر على الموقع إطلاقاً.** ولإضافة منتج للموقع
يجب تعديل كود ونشر.

### 9.2 مسار طلبات العملاء — قناة جانبية
`/api/leads` يكتب في ملف `leads.jsonl` **غير مشفّر**، ولا ينشئ أي سجل في
النظام. وجدول `Customer` يحوي أصلاً كل ما يحتاجه الطلب: `contactName`, `phone`,
`whatsapp`, `email`, `companyName`, `notes` — إضافةً إلى `CustomerActivity`.

**طلب عرض سعر من الموقع لا يصل للنظام. لا عميل، ولا نشاط، ولا عرض سعر.**

### 9.3 وحدتا نشر لمنصّة واحدة
تطبيقان يُبنيان ويُنشران منفصلين، وكلاهما يخدم `/_next/static/…` — فلا يمكن
وضعهما على نطاق واحد دون بادئة أو دمج.

---

## 10. ما يجب تغييره

| # | التصحيح | الأثر |
|---|---|---|
| 1 | دمج التطبيقين في تطبيق Next واحد بمجموعات مسارات | نطاق واحد، جلسة واحدة، `/login` هو الدخول الحقيقي |
| 2 | الموقع يقرأ المنتجات من `Product` | ينتهي التكرار؛ تغيير المدير يظهر فوراً |
| 3 | `/api/leads` ينشئ `Customer` + `CustomerActivity` | الطلب يدخل مسار البيع الحقيقي |
| 4 | إضافة `/about` `/products` `/services` | استكمال الطبقة العامة |
| 5 | خطّ نشر واحد يتحقّق من البناء والكاش وقاعدة البيانات والمصادقة | لا نجاح مُعلَن من 200 مخزَّن |

### ⚠️ عائق يحكم كل ما سبق

**المعمارية الموحّدة لا تعمل على الاستضافة الحالية.** النظام مبني على
PostgreSQL RLS، والاستضافة المشتركة لا توفّر PostgreSQL — مقيس لا مفترض.
MariaDB لا تملك RLS إطلاقاً، والنقل إليها **حذف لطبقة العزل لا ترحيل لها**.

التصحيحات 1–4 برمجية وتُنفَّذ الآن. التشغيل الفعلي لـ`/dashboard` وأخواتها
يتطلّب مضيفاً فيه PostgreSQL — VPS.

---

## ما لم يُدقَّق

- سلوك الجلسات عبر نطاقات فرعية متعددة (لا يوجد نطاق فرعي حالياً).
- أداء الموقع تحت حمل حقيقي.
- استرجاع نسخة احتياطية — **لا يوجد نسخ احتياطي مجدول أصلاً**.

---

# التصحيح المُنفَّذ — منصّة واحدة

## البنية بعد الدمج

```
apps/web/src/app/
├── (site)/     الطبقة العامة   → site.css
│   ├── page.tsx      /
│   ├── contact/      /contact
│   └── legal/        /legal/*
├── (erp)/      النظام          → erp.css
│   ├── login/        /login   ← نقطة الدخول الوحيدة
│   ├── dashboard/  sales/  admin/  portal/
│   └── … 16 وحدة أخرى
├── robots.ts   → /robots.txt
└── sitemap.ts  → /sitemap.xml
```

`apps/marketing` **حُذف**. تطبيق واحد، حزمة نشر واحدة، قاعدة بيانات واحدة،
مصادقة واحدة. و`basePath` أُزيل لأن سببه الوحيد كان تعايش تطبيقين على مضيف
واحد — والمشكلة اختفت باختفاء التطبيق الثاني.

## ما تحقّق منه فعلاً — على خادم يعمل، لا بالافتراض

| الفحص | النتيجة |
|---|---|
| الطبقة العامة على أصل واحد | `/` `/contact` `/legal/*` → 200 |
| `/robots.txt` و`/sitemap.xml` | 200 |
| مسارات النظام بلا جلسة | `/dashboard` `/sales` `/admin` `/portal` → 307 إلى `/login` |
| دخول SALES | نجح → هبط على `/sales` |
| منع SALES من `/admin` `/settings` `/dashboard` `/portal` | كلها تحويل |
| سماحه بـ`/sales/quotations` | 200 |
| **عزل أنظمة التصميم** | `--color-brand`: `#c46481` على `/` · `#5c2535` على `/sales` |
| رمز خاص بالنظام في الصفحة العامة | غير معرّف — لا تسرّب |
| lint · typecheck · build | نظيفة |
| مجموعات التحقّق | 245 تأكيداً في 9 مجموعات، كلها ناجحة |

عزل التصميم كان الخطر الأكبر في الدمج: الرمز `--color-brand` معرّف في
الملفين بقيمتين مختلفتين، ولولا تخطيطا الجذر المنفصلان لطغى أحدهما على
الآخر — ولظهر ذلك كلون خاطئ على فاتورة لا كخطأ في البناء.

## ما بقي مكرّراً — لم يُصحَّح بعد

| # | التكرار | الحالة |
|---|---|---|
| 1 | `PRODUCTS` مكتوبة في `site.ts` بدل جدول `Product` | **قائم** |
| 2 | `/api/leads` يكتب ملفاً بدل إنشاء `Customer` | **قائم** |
| 3 | `/about` `/products` `/services` غير موجودة | **قائم** |

الدمج جعل هذه التصحيحات ممكنة لأول مرة: الطبقة العامة صارت داخل التطبيق
الذي يملك Prisma والمصادقة. قبل الدمج كانت تحتاج واجهة برمجية ثانية —
أي نظاماً ثانياً.
