# UNIFIED_PLATFORM_FINAL

> 2026-08-11 · كل بند **مقيس**: من كود، أو خادم يعمل، أو طلب HTTP فعلي.
> ما لم يُختبر مكتوب أنه لم يُختبر.

---

## مُكتمل ✅

### الموقع العام — ستة مسارات، كلها 200
`/` · `/about` · `/services` · `/products` · `/contact` · `/login`
ومعها `/sitemap.xml` و`/robots.txt`.

`/about` و`/services` **بلا رقم واحد مخترع**: لا سنة تأسيس، ولا عدد موظفين،
ولا طاقة إنتاجية، ولا اسم عميل. لم يُزوَّد أيٌّ منها. محتواهما من `SERVICES`
و`WHY_KAYAN` — نفس مصدر الصفحة الرئيسية، فلا تنشأ نسختان تتباعدان.

### مصدر منتجات واحد
`lib/catalog.ts` يقرأ جدول `Product` نفسه الذي تحرّره `/catalog/products`.

**مُثبَت حيّاً على خادم يعمل:**

| التغيير في قاعدة البيانات | الأثر على `/products` |
|---|---|
| `nameAr` | ظهر فوراً — بلا نشر وبلا إعادة بناء |
| `sellingPrice` | ظهر فوراً |
| `status: INACTIVE` | اختفى المنتج فوراً |
| الإرجاع | عاد كما كان |

`force-dynamic` مقصود: صفحة ثابتة تُبنى مرة وتتجمّد، فيصير تعديل المدير غير
مرئي حتى النشر التالي — وهو التكرار نفسه بشكل آخر.

### التسعير حسب الكمية والخدمة
جدول `PriceTier` جديد: منتج · متغيّر (اختياري) · خدمة · حد أدنى · حد أعلى ·
سعر · عملة · نشط. عشر شرائح أُدخلت من أرقامك، **كلها قابلة للتعديل والحذف من
قاعدة البيانات. لا سعر في الكود.**

| الاختبار | النتيجة |
|---|---|
| تطريز، 3 قطع | 18,000 د.ع |
| تطريز، 6 فأكثر | 17,000 د.ع |
| DTF، 5 قطع | 16,000 د.ع |
| DTF، 6 فأكثر | 15,000 د.ع |
| الحدّ 5/6 لا يزحف | مضبوط |
| 10 × 15,000 | 150,000 |
| خدمة بلا شريحة | `null` لا صفر |

**عيب أصلحته:** القيد الفريد لم يكن يمنع شيئاً. PostgreSQL يعتبر كل `NULL`
مميّزاً، وشريحة المنتج كله `variantId` فيها فارغ — فكان يمرّ سعران متضاربان
لنفس الكمية بصمت. `NULLS NOT DISTINCT` أصلحه، والتحقّق يثبته بمحاولة إدخال
مكرّر ورفضه.

### مسار الطلب العام → النظام
`/api/leads` ينشئ `Customer` + `CustomerActivity` من نوع `INQUIRY`. الملف غير
المشفّر حُذف.

| الاختبار | النتيجة |
|---|---|
| طلب من الموقع | HTTP 201 |
| سجلّ العميل | `CUS-0002` أُنشئ |
| النشاط | `INQUIRY — طلب عرض سعر من الموقع` |
| الواتساب | مُلئ من الرقم |
| العربية | مخزَّنة سليمة (تحقُّق برموز يونيكود) |
| رقم مكرّر | نشاط للموجود، لا عميل ثانٍ |

يتوقف عند عميل غير مؤهَّل عمداً: عرض السعر مستند بأسعار وشروط، ولا أحد
يسعّر طلباً لم يقرأه.

### مصادقة واحدة وRBAC
| الحساب | الدور | الهبوط |
|---|---|---|
| `kayan@kayan-uniform.com` | MANAGER | `/dashboard` |
| `manager@kayan.eg` | MANAGER | `/dashboard` |
| `sales@kayan.eg` | SALES | `/sales` |
| `admin@kayan.eg` | ADMIN | `/admin` |
| `customer@kayan.eg` | CUSTOMER | `/portal` |

`/dashboard` `/sales` `/admin` `/portal` `/catalog/products` بلا جلسة → كلها
تحويل إلى `/login`. SALES مُنع فعلياً من `/admin` و`/settings` و`/dashboard`
و`/portal`، وسُمح له بـ`/sales/quotations`.

حساب المدير: كلمة صحيحة تُقبل، كلمة خاطئة تُرفض، الحساب نشط وغير مقفل.

### عزل أنظمة التصميم
`--color-brand`: `#c46481` على `/` · `#5c2535` على `/sales`. رمز خاص بالنظام
غير معرّف في الصفحة العامة. **لا تسرّب.**

---

## جزئي ⚠️

| البند | الحالة |
|---|---|
| صور المنتجات | `/products` تقرأ `ProductImage` من القاعدة؛ **الصفحة الرئيسية ما زالت تقرأ مساراتها من `site.ts`** |
| شرائح المتغيّرات | النموذج يدعمها (`variantId`)، ولم تُدخل شريحة لمتغيّر بعينه بعد |
| شاشة تحرير الشرائح | البيانات والمنطق جاهزان؛ **لا شاشة ERP لتحريرها بعد** — التعديل من قاعدة البيانات |
| اليلك التركي | بلا سعر للكميات الصغيرة — **لم تُرسل الأرقام فلم تُخترع**؛ تظهر كفجوة تغطية |

---

## مفقود ❌

- **CI**: النشر يشغّل lint وtypecheck والاختبارات كبوّابات، لكنه **لا يُطلَق من
  `git push`**. لا خادم CI.
- تكلفة شراء القطعة — **أهم رقم ناقص**. بدونه أي تقرير ربح خاطئ: النظام يحسب
  الطباعة (1,305 د.ع) ويتجاهل ثمن اليلك نفسه.
- تقارير الموظفين والرواتب والأرباح والخسائر · شاشة الكاشير.

---

## محجوب 🔴

**نظام ERP لا يمكن نشره على الاستضافة الحالية.** مقيس على الجهاز:

| العنصر | الحالة |
|---|---|
| PostgreSQL | ❌ المنفذ 5432 مغلق · `/opt/alt/postgresql11` مكتبة عميل PHP |
| المتاح | MariaDB 11.8.8 |
| RLS | ❌ مستحيل — MariaDB لا تملكها، و55 جدولاً تعتمد عليها |
| Node/Next | ✅ عبر Passenger |
| بناء Next على الخادم | ❌ `EAGAIN` (حدّ عمليات LVE) |
| SSH · متغيّرات البيئة · عملية دائمة | ✅ |

**المطلوب:** VPS · Ubuntu 22.04/24.04 · 2 vCPU · 4 GB · 40 GB SSD ·
PostgreSQL 17 · Node 20+ · nginx · certbot.

لم تُضعَّف المعمارية ولم تُستبدل قاعدة البيانات.

---

## المعماريات

**قاعدة البيانات:** PostgreSQL 17.5 · 10 هجرات · `NUMERIC(19,4)` · RLS مفروضة
على 56 جدولاً (بعد `PriceTier`) · ثلاثة أدوار، `kayan_app` بلا BYPASSRLS عمداً.
SQLite للتطوير المحلي فقط.

**المصادقة:** واحدة · Argon2id · جلسات مُجزّأة · دخول مُتساوي الزمن · قفل بعد
محاولات · 43 صلاحية · 4 أدوار · منع افتراضي. **لم تُغيَّر.**

**المنتجات والمتغيّرات:** `Product` → `ProductVariant` → `ProductImage` ·
`ProductMaterial`. اتجاه واحد إلى الموقع.

**التسعير:** `PriceTier` · اختيار الشريحة في `packages/domain/src/pricing.ts` —
شريحة المتغيّر تسبق المنتج، والنطاق الأضيق يسبق الأوسع.

**الموقع → CRM:**
`زائر → /api/leads → Customer + INQUIRY → (مراجعة بشرية) → عرض سعر → أمر بيع
→ حجز → إنتاج → تسليم → فاتورة`

**Drive:** `scripts/import-drive.mjs` قائم بلا تعديل — مجلدات متداخلة، كشف
تكرار، استيراد تزايدي، WebP، وعلاقات الصور بالمنتج والمتغيّر.

---

## النشر — والفرق الذي يجب ألا يُخلط

| ما تغيّره | كيف يصل | متى يظهر |
|---|---|---|
| **الكود** | `node scripts/deploy-site.mjs` → lint · typecheck · اختبارات · بناء · رفع · تبديل ذرّي · إعادة تشغيل · تحقّق | بعد اكتمال النشر |
| **البيانات** (منتج، سعر، حالة) | من شاشات النظام | **فوراً** — بلا إعادة بناء |

**حفظ ملف على جهازي لا يغيّر الإنتاج.** لا شيء يُنشر تلقائياً.

التحقّق يتجاوز الكاش **ويطابق بصمة البناء الحيّة** — بعد أن أعطاني الـ CDN
مرة 200 على كل مسار بينما التطبيق يرد 503. النشر في مجلد ثابت `app/`؛
`hbuilds/versions/` لم يعد مستخدماً.

---

## الفحوص

**399 تأكيداً في 15 مجموعة، كلها ناجحة** — بينها 20 تهاجم عزل المستأجرين
و12 للتسعير. `lint` و`typecheck` و`build` نظيفة بلا خطأ ولا تحذير.

---

## مخاطر قائمة

| الخطر | الحالة |
|---|---|
| **كلمة مرور حساب المدير ضعيفة ومكشوفة في المحادثة** | يجب تغييرها |
| لا CI آلي | مفتوح |
| لا نسخ احتياطي مجدول | مفتوح |
| `kayan_auth` يحمل BYPASSRLS مع DML على كل الجداول | مفتوح منذ المرحلة 7 |
| تكلفة شراء القطعة غير مُدخَلة | كل تقرير ربح خاطئ حتى تُدخل |
