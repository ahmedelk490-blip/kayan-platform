# النشر التلقائي — GitHub ← هوستنجر

الهدف: تعديل واحد يصل للإنتاج بلا SSH ولا FTP ولا رفع يدوي.

```
git push  →  هوستنجر يسحب  →  npm install  →  npm run build  →  نشر  →  حيّ
```

---

## لماذا هذه الطريقة تحديداً

| الطريقة | الحالة |
|---|---|
| SSH | ❌ المصادقة تنجح ثم تُقتل الجلسة — حدّ عمليات CloudLinux |
| SFTP | ❌ نفس السبب: لا قناة جلسة |
| FTP | ❌ يتطلب كلمة مرور مكتوبة — لا تُستخدم |
| رفع ZIP يدوي | ⚠️ يعمل، لكن بضغطة منك في كل تحديث |
| **GitHub تلقائي** | ✅ **بلا تدخّل، والبناء عند هوستنجر** |

والأهم: **الاستضافة المشتركة عاجزة عن بناء Next محلياً** — `next build` يموت بـ`EAGAIN`
عند إطلاق أول عامل، حتى مع `cpus: 1`. منظومة هوستنجر تبني في بيئة أخرى، فهي
المسار الوحيد الذي ينجح دون بناء على جهازك.

---

## الإعداد — مرة واحدة

### 1. مستودع خاص على GitHub
أنشئ مستودعاً **خاصاً** (Private). الكود يحوي مخطط قاعدة البيانات ومنطق
التسعير — ليس شيئاً يُنشر للعامة.

### 2. ادفع الكود
```bash
cd "D:/555Wooork/Cloud diangers/erp-platform"
git remote add origin https://github.com/<حسابك>/kayan-platform.git
git push -u origin main
```

### 3. اربطه في hPanel
**hPanel ← Websites ← kayan-uniform.com ← Node.js ← Create application**

- المصدر: **GitHub**
- المستودع: الذي أنشأته
- الفرع: `main`
- إصدار Node: **20** أو أحدث
- مجلد البناء: `.next`

فعّل **Auto deploy on push** إن كان الخيار ظاهراً.

### 4. متغيّرات البيئة
من **Environment Variables** في نفس الشاشة:

```
NEXT_PUBLIC_SITE_URL=https://kayan-uniform.com
LEADS_DIR=/home/u257117736/kayan-leads
DATABASE_URL=postgresql://…
DIRECT_DATABASE_URL=postgresql://…
AUTH_DATABASE_URL=postgresql://…
```

⚠️ **لا تضع أي كلمة مرور في المستودع.** هذه الشاشة هي مكانها الوحيد.

---

## بعد الإعداد

```bash
git add -A && git commit -m "وصف التعديل" && git push
```

وهذا كل شيء. هوستنجر يسحب ويبني وينشر.

---

## ⚠️ ما لن يعمل بلا PostgreSQL

الطبقة العامة تصمد وتُعرض كاملة — **مُثبَت بإيقاف قاعدة البيانات فعلياً:
ثمانية مسارات كلها 200، والصفحة الرئيسية بكامل محتواها**.

لكن هذه تبقى معطّلة حتى تتوفّر قاعدة بيانات:

| المسار | بلا PostgreSQL |
|---|---|
| `/products` والصفحة الرئيسية | تُعرض بلا منتجات |
| `/login` ولوحة المدير والنظام كله | لا يعمل |

الاستضافة المشتركة لا توفّر PostgreSQL — مقيس: المنفذ 5432 مغلق،
و`/opt/alt/postgresql11` مكتبة عميل PHP لا خادم، والمتاح MariaDB.
وMariaDB بلا Row-Level Security بينما 56 جدولاً تعتمد عليها.

**الحل:** قاعدة PostgreSQL مُدارة (Neon مجاناً) وتُضبط في متغيّرات البيئة
أعلاه، أو VPS.

---

## قبل أول دفعة — تحقّق

```bash
git ls-files | grep -E "\.env$"     # يجب ألا يُخرج شيئاً
npm run lint && npm run typecheck && npm run build
```

كلمة مرور حسابات التطوير لم تعد في المصدر: البذرة تقرأ `SEED_PASSWORD` من
البيئة وتتوقف إن غاب، فلا تُنشأ حسابات بكلمة افتراضية معروفة.
