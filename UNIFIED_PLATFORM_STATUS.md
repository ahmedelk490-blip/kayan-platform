# UNIFIED_PLATFORM_STATUS — منصّة كيان الموحّدة

> 2026-08-11 · كل بند هنا **مقيس**: من الكود، أو من خادم يعمل، أو من طلب HTTP
> فعلي. ما لم يُختبر مكتوب أنه لم يُختبر.

---

## مُكتمل ✅

### مصدر منتجات واحد
الكتالوج العام يقرأ من جدول `Product` نفسه الذي يحرّره المدير — `lib/catalog.ts`.
لا نسخة، ولا قائمة مكتوبة في الكود.

**مُثبَت حيّاً، لا مُدّعى:**

| الاختبار | النتيجة |
|---|---|
| تعديل `nameAr` في قاعدة البيانات | ظهر على `/products` فوراً، بلا نشر وبلا إعادة بناء |
| تعديل `sellingPrice` | ظهر فوراً |
| `status: INACTIVE` | اختفى المنتج من الموقع فوراً |
| الإرجاع للحالة الأصلية | عاد كما كان |

`force-dynamic` مقصود: الصفحة الثابتة تُبنى مرة وتبقى، فيصير تعديل المدير
غير مرئي حتى النشر التالي — وهو التكرار نفسه بشكل آخر.

### مسار الطلب العام → النظام
`/api/leads` صار ينشئ `Customer` حقيقياً مع `CustomerActivity` من نوع
`INQUIRY`. الملف غير المشفّر أُزيل تماماً.

| الاختبار | النتيجة |
|---|---|
| طلب عرض سعر من الموقع | HTTP 201 |
| سجلّ العميل | `CUS-0002` أُنشئ فعلاً |
| النشاط | `INQUIRY — طلب عرض سعر من الموقع` |
| الواتساب | مُلئ بنفس الرقم |
| الملاحظة | "غير مؤهَّل بعد" |
| العربية | مخزَّنة سليمة (تحقُّق برموز يونيكود) |
| رقم مكرّر | لا يُنشئ عميلاً ثانياً — يضيف نشاطاً للموجود |

**يتوقف عند عميل غير مؤهَّل عمداً.** عرض السعر مستند بأسعار وشروط، ولا أحد
يسعّر طلباً لم يقرأه. المبيعات تفتحه بعد المراجعة — قرار إنسان لا آلة.

### تطبيق واحد، نطاق واحد، مصادقة واحدة
```
app/(site)/   →  /  /products  /contact  /legal/*      (site.css)
app/(erp)/    →  /login /dashboard /sales /admin /portal /catalog/* …  (erp.css)
```
`apps/marketing` حُذف · `basePath` أُزيل · صفحة الدخول المكرّرة حُذفت.

| الاختبار | النتيجة |
|---|---|
| مسارات النظام بلا جلسة | 307 → `/login` |
| دخول SALES | هبط على `/sales` |
| منعه من `/admin` `/settings` `/dashboard` `/portal` | كلها تحويل |
| `/catalog/products` بلا جلسة | 307 → `/login` |
| عزل التصميم | `--color-brand`: `#c46481` على `/` · `#5c2535` على `/sales` |
| رمز النظام في الصفحة العامة | غير معرّف — لا تسرّب |

### حساب المدير
`kayan@kayan-uniform.com` · دور MANAGER · يهبط على `/dashboard`.
أُنشئ بنفس معاملات Argon2id التي تستخدمها البذرة.

---

## جزئي ⚠️

| البند | الحالة |
|---|---|
| `/about` و`/services` | **غير موجودتين** — `/products` أُنجزت وحدها |
| صور المنتجات العامة | الهيرو ما زال يقرأ مساراته من `site.ts`؛ `/products` وحدها تقرأ `ProductImage` |
| سعر متدرّج (خدمة × كمية) | النموذج يحمل سعراً واحداً؛ أسعارك أربعة لكل صنف |

---

## مفقود ❌

- CI حقيقي (Git → CI → نشر). النشر اليوم أمر واحد يدوي **يشغّل** lint
  وtypecheck والاختبارات كبوّابات، لكنه لا يُطلَق من `git push`.
- تقارير الموظفين والرواتب والأرباح والخسائر.
- شاشة الكاشير السريع.

---

## محجوب 🔴

**نظام ERP لا يمكن نشره على الاستضافة الحالية.** ليس تفضيلاً — مقيس على
الجهاز نفسه.

---

## تقرير البنية التحتية

| العنصر | الحالة على هوستنجر الحالي |
|---|---|
| الخطة | استضافة **مشتركة** (`u257117736`)، ليست VPS |
| محرّكات قواعد البيانات | **MariaDB 11.8.8 فقط** |
| PostgreSQL | ❌ المنفذ 5432 مغلق · `/opt/alt/postgresql11` مكتبة عميل PHP لا خادم |
| Node/Next | ✅ Node 18/20/22/24 عبر CloudLinux · Passenger يشغّل التطبيق |
| بناء Next على الخادم | ❌ `EAGAIN` عند أول عامل (حدّ عمليات LVE)، حتى مع `cpus: 1` |
| عملية دائمة | ✅ عبر Passenger |
| متغيّرات البيئة | ✅ عبر `.htaccess` (`SetEnv`) و`.env` |
| SSH | ✅ منفذ 65002 · يحدّ الجلسات بعد تتابعها |
| RLS | ❌ مستحيل — MariaDB لا تملك Row-Level Security |

### لماذا لا تصلح MariaDB
عزل المستأجرين هنا مبني على **55 جدولاً بسياسات `FORCE`** تعتمد على
`app.tenant_id`، ويُثبته 20 تأكيداً تهاجم العزل. MariaDB لا تملك هذه الميزة
إطلاقاً. النقل إليها **حذف لطبقة الأمان لا ترحيل لها**.

### المواصفة المطلوبة
| البند | الحد الأدنى |
|---|---|
| النوع | VPS بصلاحيات root |
| النظام | Ubuntu 22.04 أو 24.04 |
| المعالج/الذاكرة | 2 vCPU · 4 GB — البناء وحده يحتاج ~2 GB |
| القرص | 40 GB SSD |
| البرمجيات | PostgreSQL 17 · Node 20+ · nginx · certbot |

### المعمارية الموصى بها
نطاق واحد `kayan-uniform.com` → nginx → عملية Next واحدة (المنفذ 3300) →
PostgreSQL محلي. لا نطاق فرعي، ولا بادئة، ولا عملية ثانية.

---

## معماريات مؤكَّدة

**قاعدة البيانات:** PostgreSQL 17.5 · مخطط واحد · 8 هجرات · `NUMERIC(19,4)` ·
RLS مفروضة على 55 جدولاً · ثلاثة أدوار (`kayan_app` بلا BYPASSRLS عمداً).
SQLite للتطوير المحلي فقط ولن تُنشر.

**المصادقة:** واحدة — `lib/auth.ts` · Argon2id · رموز جلسات مُجزّأة · دخول
مُتساوي الزمن · قفل بعد محاولات. `/login` وحيدة. RBAC: 43 صلاحية، 4 أدوار،
منع افتراضي. **لم تُغيَّر.**

**تدفّق بيانات المنتج:**
`ERP /catalog/products` → جدول `Product` → `lib/catalog.ts` → `/products` العام.
اتجاه واحد، مصدر واحد.

**مسار الطلب:**
`زائر → /api/leads → Customer + INQUIRY → (مراجعة بشرية) → عرض سعر → أمر بيع
→ حجز → إنتاج → تسليم → فاتورة`

---

## نموذج التحديث الحيّ — الفرق الذي يجب ألا يُخلط

| ما تغيّره | كيف يصل للإنتاج | متى يظهر |
|---|---|---|
| **الكود** (صفحة، مكوّن، منطق) | `git commit` → `node scripts/deploy-site.mjs` → بناء ← رفع ← تبديل ← إعادة تشغيل | بعد اكتمال النشر |
| **البيانات** (منتج، سعر، حالة، عميل) | من شاشات النظام مباشرةً | **فوراً** — نفس قاعدة البيانات |

**حفظ ملف على جهازي لا يغيّر الإنتاج.** لا شيء يُنشر تلقائياً. تعديل الكود
يحتاج نشراً صريحاً، وتعديل البيانات لا يحتاج نشراً إطلاقاً.

---

## بوّابات النشر

lint · typecheck · 7 مجموعات تحقّق — **تُشغَّل قبل أي اتصال بالخادم**، والفشل
يوقف النشر. ثم بناء، رفع، تبديل ذرّي (`.next.new` ثم `mv`)، إعادة تشغيل، ثم
تحقّق **بتجاوز الكاش ومطابقة بصمة البناء**.

النشر في مجلد ثابت `app/`. `hbuilds/versions/` لم يعد مستخدماً.

---

## أخطاء ارتُكبت وصُحّحت في هذه الجولة

**تصادم `/products`.** أضفتُ الصفحة العامة فاصطدمت بشاشة إدارة المنتجات.
العنوان العام مُحدَّد صراحةً في المعمارية المعتمدة، فالإدارة انتقلت إلى
`/catalog/products` — آمن لأن `catalog/[kind]` يقبل ثلاث قيم فقط.

**كسرتُ مسارات الصور.** سكربت نقل الروابط حوّل مسارات صور الصفحة الرئيسية
(`/products/tshirts/…`) إلى `/catalog/products/…` — مجلد غير موجود. **والبناء
نجح** لأنها نصوص لا استيرادات. اكتُشفت بمراجعة الفرق، وأُعيدت، وأُضيف فحص
يمنع تكرارها.

**اختبار خاطئ كاد يُبلَّغ كعطل.** ظهرت العربية القادمة من `curl` كعلامات
استفهام في قاعدة البيانات. الخلل في أمر الاختبار على ويندوز لا في التطبيق —
الإرسال بترميز UTF-8 من ملف خزّن النص سليماً. **الاختبار كان خاطئاً، لا الكود.**

---

## مخاطر قائمة

| الخطر | الحالة |
|---|---|
| لا CI — الاختبارات بوّابة يدوية لا آلية | مفتوح |
| لا نسخ احتياطي مجدول | مفتوح |
| `kayan_auth` يحمل BYPASSRLS مع DML على كل الجداول | مفتوح منذ المرحلة 7 |
| كلمة مرور حساب المدير ضعيفة ومكشوفة في المحادثة | **يجب تغييرها** |
| تكلفة شراء القطعة غير مُدخَلة | أي تقرير ربح سيكون خاطئاً |
