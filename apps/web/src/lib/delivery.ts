/**
 * سعر التوصيل — بلا عمود جديد في قاعدة البيانات.
 *
 * «على الزبون»: بند فاتورة عادي (بلا منتج/متغيّر) بهذا الوصف، فيدخل الإجمالي
 * والطباعة والواتساب والمرتجعات كأي بند. «علينا»: مصروف «شحن وتوصيل» يُخصم
 * من الربح في التقارير — الفاتورة لا تتغيّر.
 *
 * الوصف يبدأ بـ 🚚 دائماً وهو العلامة الوحيدة المعتمدة: أسماء المنتجات لا
 * تبدأ بإيموجي، وملاحظات البائع الحرّة قد تحتوي كلمة «توصيل» فلا يُعتمد عليها.
 */
export const DELIVERY_DESCRIPTION = '🚚 أجور توصيل';

/** هل هذا وصف بند توصيل؟ — لاستثنائه من عدّ القطع وتحليل العوائل. */
export function isDeliveryDesc(description: string | null | undefined): boolean {
  return !!description && description.trim().startsWith('🚚');
}
